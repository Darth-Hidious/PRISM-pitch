# StatusTable

Who, how far along, and what exists today: one row per programme, the claim in bold and the evidence beneath it.

- State each row at the maturity it has reached. Name partners only with permission; otherwise "Industrial partner · under NDA".
- Put a SourceLine under the table.
- Collapses to stacked rows below 760 px.

Props: `rows` ({`entity`, `entityNote`, `status`: {`label`, `tone`}, `statement`: nodes[]}[]), `columns` (three headings).
