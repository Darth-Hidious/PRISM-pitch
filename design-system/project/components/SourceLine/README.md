# SourceLine

The line that says where a figure or an image comes from; every section with a number or a photograph carries one.

- `label` defaults to "Source"; use "Sources", "Illustrative", "Maturity" or a project name as needed.
- Keep it to one or two sentences, set in `ink-2`.

Props: `children`, `label`.
