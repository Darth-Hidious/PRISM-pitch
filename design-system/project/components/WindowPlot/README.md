# WindowPlot

The manufacturing window: nested regions over two process variables, the core being what can be made repeatably and a crimson point for the current optimum.

- Conceptual by definition; never plot measured data with it, and say so in the source line.
- Change the axis labels to the variables of the programme.

Props: `xLabel`, `yLabel`, `label`, `caption` (accessible description).
