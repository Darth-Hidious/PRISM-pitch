# CapabilityStack

The platform as numbered layers, each with what it does and its maturity: ambition and evidence on the same line.

- Order layers from what the customer asks for to what they receive.
- Every layer carries a maturity; never leave one blank.

Props: `layers` ({`name`, `detail`, `maturity`}[]), `label`.
